# conftest.py — placing this file at the project root causes pytest to add
# the root directory to sys.path automatically, making `core`, `adapters`,
# `exporters`, `utils`, etc. importable from any test module.
